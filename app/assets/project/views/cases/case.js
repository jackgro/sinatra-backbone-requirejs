define(['backbone'], function(Backbone) {
  Tenure.views.CaseView = Backbone.View.extend({
    initialize: function() {
      console.log('Inside Case View!');
    }
  });

  return Tenure.views.CaseView;
});
